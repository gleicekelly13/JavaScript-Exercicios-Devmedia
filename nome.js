let nome_composto = "Gleice Kelly";

let array = nome_composto.split(" ");

let nome = array[0];

console.log("Olá " + nome + ", seja bem vinda");

/* Obtemos o primeiro nome da string nome_composto.Com o método split() separamos a string em diversas substring e depois acessamos o primeiro índice do array, que contém o nome que desejamos.
*/
