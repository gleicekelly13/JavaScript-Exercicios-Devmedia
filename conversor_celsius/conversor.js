function converteParaCelsius(fahrenheit) {
    let celsius = (fahrenheit - 32) / 1.8;
    return celsius;
}

export default converteParaCelsius;

//Camada que armazena a lógica de conversão de temperatura da aplicação
