const imprime_itens = (series) => {
    
    for (let serie of series) {
        console.log(serie);
    }
}

let series = ['Greys anatomy', 'Station 19', 'Suits', 'Apostando Alto',];

imprime_itens(series);
