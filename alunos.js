let alunos = ["Carlos", "Pablo", "Juliana", "Ingrid"];

let total_alunos = alunos.length; //Total de alunos que estão no array.

for(let contador = 0; contador < total_alunos; contador++) {

    console.log(alunos[contador]); //Imprime o nome do aluno a cada iteração
}

//-----------------------------------------------
//WHILE
/* = 0;

while(contador < alunos.length) { //Acada execução do bloco será impresso o nome do aluno que está na posição contador no array alunos.

    let aluno_nome = alunos[contador];

    console.log("Aluno: " + aluno_nome);
    contador++;
} */
